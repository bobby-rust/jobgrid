import { JobApplication } from '@/types/api';
import React from 'react'

type Props = {
    data: JobApplication[];
}

export default function JobApplicationTable({ data }: Props) {
    return (
        <div>job-application-table</div>
    )
}