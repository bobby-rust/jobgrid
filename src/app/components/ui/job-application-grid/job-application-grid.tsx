import { JobApplication } from '@/types/api';
import React from 'react'

type Props = {
    data: JobApplication[];
}

export default function JobApplicationGrid({ }: Props) {
    return (
        <div>Job Application Grid</div>
    )
}